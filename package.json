{
  "name": "liri",
  "version": "1.0.0",
  "bin": {
    "liri": "./liri.js"
  },
  "description": "LIRI is like SIRI.  It is a command line NodeJS script that takes in parameters and outputs data.",
  "main": "liri.js",
  "dependencies": {
    "request": "^2.71.0",
    "twitter": "^1.2.5",
    "spotify": "^0.3.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
